import { MultisearchinputPipe } from './multisearchinput.pipe';

describe('MultisearchinputPipe', () => {
  it('create an instance', () => {
    const pipe = new MultisearchinputPipe();
    expect(pipe).toBeTruthy();
  });
});
